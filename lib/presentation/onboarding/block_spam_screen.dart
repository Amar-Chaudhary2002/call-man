import 'package:flutter/material.dart';

class BlockSpamScreen extends StatelessWidget {
  const BlockSpamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
